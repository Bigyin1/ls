#  GNU ls Implementation


* Implemented flags:  -l, -R, -a, -r, -t, -1, --help


## Installation
```
mkdir build && cd build
cmake ..
./ls {optional argument}
```
Tested on Ubuntu 20.04 and MacOS Catalina
